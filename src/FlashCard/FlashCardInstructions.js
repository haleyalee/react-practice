import React from 'react'

function FlashCardInstructions() {
  return (
    <div>
      <p>
        Build a web app that display an interactive flashcard and provides basic navigation for 
        going foward or backward through the pack of cards
      </p>
      <ul>
        <li>What data structure would you use?</li>
        <li>Create the HTML markup</li>
        <li>Write logic for clicking next or previous buttons</li>
      </ul>
    </div>
  )
}

export default FlashCardInstructions
